<h2>ジャンル編集</h2>
<%= form_with model:@genre, url: admin_genre_path do |f| %>
  <div class="field">
    <%= f.label :ジャンル名 %>
    <%= f.text_field :name, autofocus: true%>
  </div>
  <%= f.submit "変更を保存" %>
<% end %>